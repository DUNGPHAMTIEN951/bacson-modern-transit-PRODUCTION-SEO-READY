# Public client configuration only. Never place passwords, OAuth secrets, service-account keys,
# or other private credentials in VITE_* variables because they are bundled into browser code.
#
# Google Apps Script Web App endpoint used by the callback/booking consultation form.
# Setup guide: docs/GOOGLE_SHEETS_LEADS_SETUP.md
VITE_BOOKING_FORM_ENDPOINT=https://script.google.com/macros/s/XXXXX/exec
